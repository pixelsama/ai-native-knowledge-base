# Learning

Store interactive learning artifacts such as HTML explainers, visualizations,
quizzes, and small labs.

Any directory containing `index.html` must also contain:

- `manifest.json`
- `sources.md`
